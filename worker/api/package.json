{
  "name": "ramme-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply ramme --local",
    "db:migrate:remote": "wrangler d1 migrations apply ramme --remote",
    "test": "node --test test/lib/*.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
